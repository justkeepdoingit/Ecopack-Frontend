export interface truckModel {
    id?: number
    name: string,
    plate: string,
    desc: string,
    ban: string,
    capacity: number
}

export interface truckSelect {
    value?: number
    viewValue: string
}